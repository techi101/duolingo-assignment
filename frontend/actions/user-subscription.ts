"use server";

// Stripe is not used in this assignment – mocked as per requirements
export const createStripeUrl = async () => {
  // Return a placeholder — the UI shows "Coming Soon" behaviour
  return { data: null, error: "Subscriptions not available in this demo" };
};
